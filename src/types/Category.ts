/**
 * カテゴリのinterface
 * @package types
 */

/**
 * カテゴリ共通モデル
 */
export interface CategoryType {
  id: string
  name: string
  slug: string
  createdAt: string
  updatedAt: string
  publishedAt: string
  revisedAt: string
}
